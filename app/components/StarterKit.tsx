import { ReactElement } from "react";

function StarterKit(): ReactElement {
    return (
			<article className="mb-8 text-center">
				<h2 className="app__title text-4xl font-semibold">Drift Off Course</h2>
			</article>
		);
}

export default StarterKit
